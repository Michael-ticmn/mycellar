// Phase 3 stub. Camera capture (getUserMedia, rear camera), upload to Supabase
// Storage `bottle-labels/{user_id}/...`, insert scan_request, subscribe to
// scan_responses. See BUILD_SPEC §3.1 / §3.1c.

export async function captureLabel(_intent) {
  throw new Error('scan: not yet implemented (Phase 3)');
}
